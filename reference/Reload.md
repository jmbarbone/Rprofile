# Reload

Reloads your session

## Usage

``` r
.Reload(remove_objects = TRUE, loud = FALSE)

.Restart()
```

## Arguments

- remove_objects:

  Logical, if `TRUE` will remove all objects found

- loud:

  Logical, if `TRUE` will present a message on removed objects
