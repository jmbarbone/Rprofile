# Spell check a file

Spell check a file

## Usage

``` r
.SpellCheckFile(path = NULL)
```

## Arguments

- path:

  A path to spell check; if `NULL` check the current file open in the
  RStudio editor
