# Wrapper for `{pak::pak}`

Wrapper for `{pak::pak}`

## Usage

``` r
.Pak(...)
```

## Arguments

- ...:

  Arguments passed to
  [`pak::pak()`](https://pak.r-lib.org/reference/pak.html).

## Details

If the envvar `R_LIBS_PAK` is set then it will be used as the library
path for `{pak}`
